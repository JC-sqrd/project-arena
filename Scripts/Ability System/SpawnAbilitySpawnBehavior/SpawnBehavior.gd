class_name SpawnBehavior
extends Resource

var hit_listener : HitListener
var aim_at_mouse : bool = true

signal spawn_finished()

func spawn(ability : Ability, actor : Entity, spawn_position : Vector2, spawn_direction : Vector2, spawn_scene : PackedScene):
	
	pass
